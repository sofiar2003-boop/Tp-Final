# Puerto en el que corre tu servidor local
PORT=8080

# Conexión a la Base de Datos (Ejemplo con MongoDB)
MONGO_URI=mongodb://127.0.0.1:27017/tracker_habitos

# Clave secreta para firmar los tokens JWT (Cualquier texto largo y seguro)
JWT_SECRET=MiClaveSuperSecretaYEscondida123!
JWT_EXPIRES_IN=7d

# Configuración de Nodemailer (Para mandar los mails de verificación)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=judy.mckenzie@ethereal.email
SMTP_PASS=VTY89SG92wxDaRpcpT

# configuro el tiempo de expiración del token de verificación (ejemplo: 1 hora)
JWT_SECRET=MiPalabraClaveSuperSecretaDeLaUTN123!
JWT_EXPIRES_IN=2h